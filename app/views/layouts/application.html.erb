<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width,initial-scale=1, maximum-scale=1">

    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand" href="/"><span>Bookers</span></a>
          <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="true" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="navbar-collapse collapse" id="navbarNavDropdown" style="">
           <!--true→Users(users)・Books(books)・Logout(sign_out)-->
            <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <a class="fas fa-home nav-link text-light"></a><!--Homeのアイコン-->
                <%= link_to "Home", user_path(current_user), class: "nav-link text-light" %><!--Home(users/show)リンク-->
              </li>
              <li class="nav-item">
                <a class="fas fa-users nav-link text-light"></a><!--Usersのアイコン-->
                <%= link_to "Users", users_path, class: "nav-link text-light" %><!--User(users)リンク-->
              </li>
              <li class="nav-item">
                <a class="fas fa-book-open nav-link text-light"></a><!--Booksのアイコン-->
                <%= link_to "Books", books_path, class: "nav-link text-light" %><!--Books(books)リンク-->
              </li>
              <li class="nav-item">
                <a class="fas fa-sign-out-alt nav-link text-light"></a><!--Log outのアイコン-->
                <%= link_to "Log out", destroy_user_session_path, class: "nav-link text-light", method: :delete %><!--Log out(リンク-->
              </li>

            <% else %>
              <li class="nav-item">
                <a class="fas fa-home nav-link text-light"></a><!--Homeのアイコン-->
                <%= link_to "Home", root_path, class: "nav-link text-light" %><!--Home(users/show)リンク-->
              </li>
              <li class="nav-item">
                <a class="fas fa-link nav-link text-light"></a>
                <%= link_to "About", about_path, class: "nav-link text-light" %>
              </li>
              <li class="nav-item">
                <a class="fas fa-user-plus nav-link text-light"></a>
                <%= link_to "Sign up", new_user_registration_path, class: "nav-link text-light" %>
              </li>
              <li class="nav-item">
                <a class="fas fa-sign-in-alt nav-link text-light"></a>
                <%= link_to "Log in", new_user_session_path, class: "nav-link text-light" %>
              </li>
            </ul>
          </div>
        </div>
    <% end %>
      </nav>
    </header>

    <% if flash[:notice] %>
    <p id="notice"><%= flash[:notice] %></p>
    <% end %>
    

    <main>
      <div class="container px-5 px-sm-0">

        <div class="row">

    <%= yield %>

        </div>
      </div>
    </main>

    <footer>
      <div class="container mt-5">
        <div class="row">
          <div class="mx-auto">
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>

